# AI-Analyst

Static prototype for a Noon-inspired Tech Care CS AI analyst.

## Run locally

Open `index.html` in a browser, or serve the folder with:

```bash
python3 -m http.server 4173
```

Then visit `http://localhost:4173`.
